                         ┌───────────────────────────────┐
                         │           HUMAN (HIL)         │
                         │  Expert / pilot team / user   │
                         └───────────────┬───────────────┘
                                         │ (labels / feedback)
                                         v
┌──────────────────────────────┐   ┌───────────────────────────────┐
│   Dashboard / Labeling UI    │   │   Active Learning Selector    │
│  (choose what to label)      │<--│  strategy: random/uncertainty │
└──────────────┬───────────────┘   └───────────────┬───────────────┘
               │ (append new labels)               │ (needs model preds)
               v                                   v
┌────────────────────────────────────────────────────────────────────┐
│                               MinIO                                │
│  Bucket: smart-energy-results (primer)                             │
│   - al_training_dataset/simulation_security_labels_n-1_*.csv       │
|   - al_training_dataset/simulation_security_labels_n-1_latest.csv  │
│   - models/<prefix>/<run_id>/model.joblib                          │
│   - models/<prefix>/<run_id>/metrics.json                          │
└──────────────┬───────────────────────────────────────┬─────────────┘
               │                                       │
               │ baseline + latest                     │ model + metrics out
               v                                       ^
┌──────────────────────────────┐   train/retrain   ┌───┴─────────────────────┐
│     Retraining API (FastAPI) │<------------------│   ML Training Pipeline  │
│  GET /health                 │                   │  RandomForest + eval    │
│  POST /retrain               │------------------>|  save artifacts         │
└──────────────┬───────────────┘                   └─────────────────────────┘
               │
               │ (optional) serve latest model for scoring
               v
┌──────────────────────────────┐
│  Inference / Scoring Service │
│  (predict + confidence)      │
└──────────────┬───────────────┘
               │ low-confidence samples
               v
      (back to Dashboard / AL Selector)
